// src/lib/ads.ts
// ─────────────────────────────────────────────────────────────
// 광고 설정 — 여기 한 곳만 고치면 사이트 전체에 반영된다.
// ─────────────────────────────────────────────────────────────
//
// 【사용자가 할 일】
//   忍者AdMax에서 발급받은 태그의 "긴 영문/숫자 부분"만 아래에 붙여넣으면 된다.
//
//   예) 발급된 태그가 이것이라면
//       <script src="https://adm.shinobi.jp/s/1a006271daee325da114c4fe296d8187"></script>
//                                             └────────── 이 부분만 ──────────┘
//
// 【광고를 잠시 끄고 싶을 때】
//   Vercel 환경변수에 NEXT_PUBLIC_ADMAX_ENABLED=0 을 넣으면 전부 사라진다.
//   (애드센스 심사 중에는 끄는 것을 권장 — 광고가 많으면 심사에 불리하다)

export const ADMAX = {
  /** PC(넓은 화면)용 광고 ID */
  pc: '1a006271daee325da114c4fe296d8187',
  /** 스마트폰용 광고 ID */
  sp: '818e12891c2828177d5815e4e695b503',
} as const;

/**
 * 광고 표시 여부.
 * 환경변수가 '0' 이면 끈다. 값이 없으면 켜진 것으로 본다.
 * 애드센스 심사 기간에는 Vercel에서 0으로 바꿔두는 것을 권장.
 */
export const ADMAX_ENABLED =
  process.env.NEXT_PUBLIC_ADMAX_ENABLED !== '0';

/**
 * 광고 크기.
 * 忍者AdMax에서 만든 광고 틀의 크기와 반드시 맞춰야 한다.
 * (다르면 빈 공간이 생기거나 잘린다)
 */
export const ADMAX_SIZE = {
  pc: { width: 300, height: 250 },
  sp: { width: 320, height: 100 },
} as const;
